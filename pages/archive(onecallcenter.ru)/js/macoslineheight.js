$(document).ready(function() {
    var $menu = $('.menu-mob');

    if (navigator.userAgent.match(/Mac OS X/)) {
      $('.languages li span').css('line-height', '17px');
    }

    // $('.nav-mob-menu').on('click', function() {
    //   $menu.fadeToggle(600);
    // });
});